# See https://github.com/Roave/BackwardCompatibilityCheck

name: "Backwards Compatibility Check"

on:
    pull_request: ~

jobs:
    backwards-compatibility-check:
        name: "Backwards Compatibility Check"

        runs-on: "ubuntu-latest"

        steps:
            -   name: "Checkout"
                uses: "actions/checkout@v4"
                with:
                    fetch-depth: 0

            -   name: "Setup PHP, with composer and extensions"
                uses: "shivammathur/setup-php@v2"
                with:
                    php-version: "8.1"
                    coverage: "none"

            -   name: "Install tool"
                run: "composer global require roave/backward-compatibility-check"

            # Nothing has been released yet, so backward-compatibility breaks are expected as the plugin's
            # interfaces take shape. The check still runs and annotates every break for visibility, but it
            # must not fail the build until the first tagged release, when this should become blocking again.
            -   name: "Check for BC breaks"
                continue-on-error: true
                run: "~/.composer/vendor/bin/roave-backward-compatibility-check --from=origin/${{ github.event.pull_request.base.ref }} --format=github-actions"
