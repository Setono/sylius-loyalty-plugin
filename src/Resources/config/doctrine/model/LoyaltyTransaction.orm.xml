<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>
    <!--
        The discriminator map is built at runtime by LoyaltyTransactionDiscriminatorMapListener from
        the registered transaction resources (%sylius.resources%), so projects can add a transaction
        type just by registering it as a resource.
    -->
    <entity name="Setono\SyliusLoyaltyPlugin\Model\LoyaltyTransaction" table="setono_sylius_loyalty__loyalty_transaction" inheritance-type="SINGLE_TABLE">
        <indexes>
            <index name="setono_sylius_loyalty__transaction_replay_idx" columns="account_id,occurred_at,id" />
            <index name="setono_sylius_loyalty__transaction_expiry_idx" columns="account_id,expires_at" />
        </indexes>
        <unique-constraints>
            <unique-constraint name="setono_sylius_loyalty__transaction_account_type_order_uniq" columns="account_id,type,order_id" />
            <unique-constraint name="setono_sylius_loyalty__transaction_type_earn_uniq" columns="type,earn_id" />
            <unique-constraint name="setono_sylius_loyalty__transaction_account_type_source_uniq" columns="account_id,type,source_identifier" />
        </unique-constraints>

        <id name="id" column="id" type="integer">
            <generator strategy="AUTO" />
        </id>

        <discriminator-column name="type" type="string" />

        <many-to-one field="account" target-entity="Setono\SyliusLoyaltyPlugin\Model\LoyaltyAccountInterface">
            <join-column name="account_id" referenced-column-name="id" nullable="false" on-delete="CASCADE" />
        </many-to-one>

        <field name="points" column="points" type="integer" />
        <field name="occurredAt" column="occurred_at" type="datetime" />
    </entity>
</doctrine-mapping>
