<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Setono\SyliusLoyaltyPlugin\Model\ClawbackLoyaltyTransaction">
        <!-- The order reference is informational and deliberately mapped to its own column:
             cancelling a referee's qualifying order claws back both their earn_order and their
             earn_referral credit - two clawbacks referencing the same order, which the
             (account_id, type, order_id) unique constraint would wrongly reject if this used
             the shared order_id column -->
        <many-to-one field="order" target-entity="Sylius\Component\Core\Model\OrderInterface">
            <join-column name="clawback_order_id" referenced-column-name="id" nullable="true" on-delete="SET NULL"/>
        </many-to-one>

        <many-to-one field="earn" target-entity="Setono\SyliusLoyaltyPlugin\Model\CreditLoyaltyTransaction">
            <join-column name="earn_id" referenced-column-name="id" nullable="true" on-delete="CASCADE"/>
        </many-to-one>
    </entity>

</doctrine-mapping>
